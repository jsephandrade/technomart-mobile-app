# TechnoMart Mobile App

Expo + NativeWind login/sign-up UI.

## Setup

1. Install dependencies

   ```bash
   npm i
   ```

2. Start the app

   ```bash
   npx expo start
   ```

Replace `assets/logo.png` with your logo image.
